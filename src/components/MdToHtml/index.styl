@import '../../Styles/variable.styl'
.md-to-html >>> {
  .hljs {
    color $fontColor
    background-color rgba(255, 255, 255, 0.35)
  }
  p {
    code {
      color $baseColor
      font-weight bold
    }
  }
}